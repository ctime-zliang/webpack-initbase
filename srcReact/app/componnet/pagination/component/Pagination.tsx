import React from 'react'
import { TPagination } from '../types/types'

type TPaginationProps = TPagination

function Pagination(props: TPaginationProps): React.ReactElement {
	return <span>111</span>
}
